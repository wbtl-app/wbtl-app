# WBTL App Scripts Configuration
# Copy this file to .env and fill in your values
#
# DO NOT commit the .env file - it is git-ignored

# =============================================================================
# Project Directory (used by setup-repo.sh)
# =============================================================================
# The directory where new tool repositories will be created
# Use $HOME to reference your home directory
PROJECTS_DIR="$HOME/projects/wbtl-app"

# =============================================================================
# Tool Specs Directory (used by setup-repo.sh)
# =============================================================================
# The directory containing tool spec files (.md and .svg)
# Each tool requires both <tool-name>.md and <tool-name>.svg to exist
TOOL_SPECS_DIR="$HOME/projects/wbtl-app/specs-wbtl/tool-specs"

# =============================================================================
# Cloudflare Credentials (used by deploy-page.sh)
# =============================================================================

# Your Cloudflare account ID
# Find it in the URL: dash.cloudflare.com/<account-id>
CLOUDFLARE_ACCOUNT_ID=

# API Token with Pages and DNS edit permissions
# Create at: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=

# Zone ID for the wbtl.app domain
# Find it on the domain overview page under "API" section
CLOUDFLARE_ZONE_ID=
